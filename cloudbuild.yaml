steps:
  # ==================================================================
  # Step 0: Gradle 캐시 복원 (속도 최적화)
  # ==================================================================
  - name: 'gcr.io/cloud-builders/gsutil'
    id: 'Restore Cache'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gsutil cp gs://${_DEST_BUCKET}/cache/gradle_cache.tar.gz /tmp/cache.tar.gz || echo "No cache found"
        if [ -f /tmp/cache.tar.gz ]; then
          tar -xzf /tmp/cache.tar.gz -C /root/
          echo "✅ Cache restored."
        fi

  # ==================================================================
  # Step 1: config.json 동적 생성 및 유효성 검사 (환경 변수 방식 적용)
  # ==================================================================
  - name: 'ubuntu'
    id: 'Create Config'
    # [핵심] Cloud Build 변수를 리눅스 환경 변수로 매핑 (가장 안전한 방법)
    env:
      - 'PROJECT_ID=${_PROJECT_ID}'
      - 'APP_NAME=${_APP_NAME}'
      - 'PACKAGE_NAME=${_PACKAGE_NAME}'
      - 'LOADING_URL=${_LOADING_URL}'
      - 'VERSION_NAME=${_VERSION_NAME}'
      - 'VERSION_CODE=${_VERSION_CODE}'
    script: |
      echo "🔍 Validating inputs..."
      
      # 환경 변수($VAR)를 검사
      if [ -z "$PROJECT_ID" ]; then echo "❌ Error: PROJECT_ID is missing!"; exit 1; fi
      if [ -z "$APP_NAME" ]; then echo "❌ Error: APP_NAME is missing!"; exit 1; fi
      if [ -z "$PACKAGE_NAME" ]; then echo "❌ Error: PACKAGE_NAME is missing!"; exit 1; fi
      if [ -z "$LOADING_URL" ]; then echo "❌ Error: LOADING_URL is missing!"; exit 1; fi
      if [ -z "$VERSION_NAME" ]; then echo "❌ Error: VERSION_NAME is missing!"; exit 1; fi
      if [ -z "$VERSION_CODE" ]; then echo "❌ Error: VERSION_CODE is missing!"; exit 1; fi
      
      echo "✅ All inputs are valid. Creating config.json..."
      
      # 환경 변수를 사용하여 JSON 생성
      cat <<EOF > app/config.json
      {
        "projectId": "$PROJECT_ID",
        "appName": "$APP_NAME",
        "packageName": "$PACKAGE_NAME",
        "loadingUrl": "$LOADING_URL",
        "appVersionName": "$VERSION_NAME",
        "appVersionCode": $VERSION_CODE
      }
      EOF

      # [검증] 파일 생성 확인
      if [ ! -s app/config.json ]; then
        echo "❌ Error: Failed to create config.json"
        exit 1
      fi
      
      # 생성된 내용 확인
      cat app/config.json

  # ==================================================================
  # Step 2: 앱 아이콘 다운로드 (실패 시 중단)
  # ==================================================================
  - name: 'gcr.io/cloud-builders/curl'
    id: 'Download Icon'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Downloading icon from ${_APP_ICON_URL}..."
        mkdir -p app/src/main/res/mipmap-xxxhdpi
        
        # curl 실패 시(-f) 에러 코드 리턴
        if ! curl -f -L -o app/src/main/res/mipmap-xxxhdpi/ic_launcher.png "${_APP_ICON_URL}"; then
           echo "❌ Error: Failed to download icon from ${_APP_ICON_URL}"
           exit 1
        fi
        
        # 파일 크기 0 체크
        if [ ! -s app/src/main/res/mipmap-xxxhdpi/ic_launcher.png ]; then
           echo "❌ Error: Icon file is empty"
           exit 1
        fi

        cp app/src/main/res/mipmap-xxxhdpi/ic_launcher.png app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.png
        echo "✅ Icon downloaded successfully."

  # ==================================================================
  # Step 2-A: 앱 서명키 확인 및 다운로드
  # ==================================================================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Check/Download Keystore'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        KEY_SECRET_ID="keystore-${_PROJECT_ID}"
        echo "🔐 Checking Secret Manager for: $$KEY_SECRET_ID"
        
        # 권한 에러 등으로 실패할 수 있으나, '없음'과 '권한없음'을 구분하기 위해
        # 여기서는 실패하더라도 다음 단계(생성 시도)로 넘어가되, 최종적으로 키 파일이 없으면 중단하도록 설계
        if gcloud secrets versions access latest --secret="$$KEY_SECRET_ID" --out-file="app/keystore.jks"; then
          echo "✅ Keystore found and downloaded."
        else
          echo "⚠️ Keystore not found (or permission denied). Will attempt to create new one."
          touch create_new_keystore 
        fi

  # ==================================================================
  # Step 2-B: 앱 서명키 생성 (없는 경우)
  # ==================================================================
  - name: 'asia-northeast3-docker.pkg.dev/appbox-479702/appbox-artifact-registry/android-builder:latest'
    id: 'Generate Keystore'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if [ -f create_new_keystore ]; then
          echo "🆕 Generating new keystore..."
        
          SALT="mobpa_secret_salt_v1"
          RAW_HASH=$(echo -n "${_PROJECT_ID}$$SALT" | sha256sum)
          ALIAS="key_$(echo -n $$RAW_HASH | cut -c1-10)"
          PASS="$(echo -n $$RAW_HASH | cut -c11-26)A1!"
        
          echo "Generated Alias: $$ALIAS"
        
          # keytool 실패 시 즉시 종료 (set -e 효과)
          if ! keytool -genkeypair -v \
            -keystore app/keystore.jks \
            -alias "$$ALIAS" \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass "$$PASS" \
            -keypass "$$PASS" \
            -dname "CN=${_APP_NAME}, OU=AppBox, O=MobPa, L=Seoul, C=KR"; then
             echo "❌ Error: Failed to generate keystore"
             exit 1
          fi
        
          echo "$$ALIAS" > keystore_alias
          echo "$$PASS" > keystore_pass
        
          echo "✅ New keystore generated."
        fi

  # ==================================================================
  # Step 2-C: 생성된 키 Secret Manager 업로드
  # ==================================================================
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Upload Keystore Secret'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        if [ -f create_new_keystore ]; then
          KEY_SECRET_ID="keystore-${_PROJECT_ID}"
          echo "📤 Uploading new keystore to Secret Manager..."
        
          # 업로드 실패 시 경고만 출력 (빌드는 계속 진행 가능하도록)
          gcloud secrets create "$$KEY_SECRET_ID" --replication-policy="automatic" || true
          if ! gcloud secrets versions add "$$KEY_SECRET_ID" --data-file="app/keystore.jks"; then
             echo "⚠️ Warning: Failed to save keystore to Secret Manager. Build will continue but key is not backed up."
          else
             echo "✅ Keystore saved to Secret Manager."
          fi
        fi

  # ==================================================================
  # [New] Step 2-Check: 필수 파일 최종 확인 (이거 없으면 빌드 안 함)
  # ==================================================================
  - name: 'ubuntu'
    id: 'Pre-Build Check'
    script: |
      echo "🔍 Verifying required files..."
      
      if [ ! -s app/config.json ]; then echo "❌ Critical Error: app/config.json is missing!"; exit 1; fi
      if [ ! -s app/src/main/res/mipmap-xxxhdpi/ic_launcher.png ]; then echo "❌ Critical Error: App icon is missing!"; exit 1; fi
      if [ ! -s app/keystore.jks ]; then echo "❌ Critical Error: Keystore file is missing!"; exit 1; fi
      
      echo "✅ All required files are present. Proceeding to build."

  # ==================================================================
  # Step 2-D: Gradle에 서명 정보 주입
  # ==================================================================
  - name: 'ubuntu'
    id: 'Inject Signing Config'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "💉 Injecting signing configuration..."
        GRADLE_FILE="app/build.gradle.kts"
        
        if [ -f create_new_keystore ]; then
          ALIAS=$(cat keystore_alias)
          PASS=$(cat keystore_pass)
        else
          SALT="mobpa_secret_salt_v1"
          RAW_HASH=$(echo -n "${_PROJECT_ID}$$SALT" | sha256sum)
          ALIAS="key_$(echo -n $$RAW_HASH | cut -c1-10)"
          PASS="$(echo -n $$RAW_HASH | cut -c11-26)A1!"
        fi
        
        sed -i "s|__KEYSTORE_PATH__|keystore.jks|g" $$GRADLE_FILE
        sed -i "s|__KEYSTORE_STORE_PASSWORD__|$$PASS|g" $$GRADLE_FILE
        sed -i "s|__KEY_ALIAS__|$$ALIAS|g" $$GRADLE_FILE
        sed -i "s|__KEY_PASSWORD__|$$PASS|g" $$GRADLE_FILE
        
        echo "✅ Signing config injected."

  # ==================================================================
  # Step 2-E: 프로젝트 소스 백업
  # ==================================================================
  - name: 'gcr.io/cloud-builders/gsutil'
    id: 'Archive Source'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "📦 Archiving project source..."
        tar -czf /tmp/source_backup.tar.gz --exclude=.git --exclude=.gradle --exclude=app/build \
          --exclude=create_new_keystore --exclude=keystore_alias --exclude=keystore_pass .
        
        RAW_GCS_PATH="${_OUTPUT_GCS_PATH}"
        GCS_ROOT="$${RAW_GCS_PATH%/}"
        TARGET_PATH="$$GCS_ROOT/${_VERSION_NAME}/source/"
        echo "📤 Uploading source to: $$TARGET_PATH"
        gsutil cp /tmp/source_backup.tar.gz "$$TARGET_PATH"

  # ==================================================================
  # Step 3: 안드로이드 빌드
  # ==================================================================
  - name: 'asia-northeast3-docker.pkg.dev/appbox-479702/appbox-artifact-registry/android-builder:latest'
    id: 'Build Artifacts'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        chmod +x gradlew
        ./gradlew assembleRelease bundleRelease --build-cache

  # ==================================================================
  # Step 4: Gradle 캐시 저장
  # ==================================================================
  - name: 'gcr.io/cloud-builders/gsutil'
    id: 'Save Cache'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "💾 Saving Gradle cache..."
        tar -czf /tmp/cache.tar.gz -C /root/ .gradle
        gsutil cp /tmp/cache.tar.gz gs://${_DEST_BUCKET}/cache/gradle_cache.tar.gz
        echo "✅ Cache saved."

  # ==================================================================
  # Step 5: 결과물 업로드
  # ==================================================================
  - name: 'gcr.io/cloud-builders/gsutil'
    id: 'Upload Artifacts'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "🚀 Uploading artifacts..."
        
        RAW_GCS_PATH="${_OUTPUT_GCS_PATH}"
        GCS_ROOT="$${RAW_GCS_PATH%/}"
        
        BASE_PATH="$$GCS_ROOT/${_VERSION_NAME}"
        APK_PATH="$$BASE_PATH/apk/"
        AAB_PATH="$$BASE_PATH/aab/"
        
        echo " - APK Path: $$APK_PATH"
        echo " - AAB Path: $$AAB_PATH"
        
        gsutil cp app/build/outputs/apk/release/*.apk "$$APK_PATH"
        gsutil cp app/build/outputs/bundle/release/*.aab "$$AAB_PATH"

# 변수 정의
substitutions:
  _PROJECT_ID: "AAA-000000"
  _APP_NAME: "앱박스"
  _PACKAGE_NAME: "kr.co.mobpa.sample.waveAppSuiteSdk"
  _LOADING_URL: "https://www.appboxapp.com/"
  _VERSION_NAME: "1.0.1"
  _VERSION_CODE: "1"
  _APP_ICON_URL: "https://via.placeholder.com/150"
  _OUTPUT_GCS_PATH: "gs://my-bucket/output/"
  _DEST_BUCKET: "my-bucket"

timeout: 3600s

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY